int funcao(int a, int b) {
	return a + b * ;
}

int main() {
	int a = 2,  = 5;

	int res  funcao(a, é);

	if (res) {
		for (res; res >= 0; res--) {
			printf("res: %d\n", res);
		}
	} else {
		res += 30;
		while (res > 20) {
			res--;
		}

		do {
			res -= 2;
		}  (res);
	}

	printf("O resultado é: %d\n", res)

	return 0;
}
